{
   "name": "horizn",
   "version": "2.0.0-7",
   "description": "Horizn's focus is to make the web's digital publication format (blogs) more paletable to users of all device sizes. It also pays a great deal of attention to semantic HTML. Horizn shows your visitors what they need to see, when they need to see it, regardless of what kind of device they are on, or what orientation they are holding it in. Horizn is the default layout for [GHK](https://github.com/rjminchuk/ghk).",
   "main": "horizn.css",
   "scripts": {
      "clean": "rm -r dist; mkdir -p dist/css",
      "less": "for file in src/*.less; do ./node_modules/less/bin/lessc $file dist/css/$(basename $file | sed -e s/less/css/g); done;",
      "min": "for file in dist/css/*.css; do ./node_modules/clean-css-cli/bin/cleancss --output dist/css/$(basename $file | sed -e s/\\.css/\\.min\\.css/g) $file; done;",
      "all": "./node_modules/npm-run-all/bin/npm-run-all/index.js clean less min",
      "docker": "npm run all; set -e; docker build --no-cache --tag horizn .; docker run --detach --rm --publish 8080:80 horizn; read; docker stop horizn;"
   },
   "repository": {
      "type": "git",
      "url": "git+https://github.com/rjminchuk/horizn.git"
   },
   "keywords": [
      "horizn",
      "digital",
      "publication",
      "format",
      "strategy",
      "mobile+first",
      "blog",
      "article",
      "semantic+html",
      "css"
   ],
   "author": "Rich Minchuk",
   "license": "MIT",
   "bugs": {
      "url": "https://github.com/rjminchuk/horizn/issues"
   },
   "homepage": "https://github.com/rjminchuk/horizn#readme",
   "dependencies": {},
   "devDependencies": {
      "clean-css-cli": "^4.3.0",
      "less": "^4.0.0",
      "npm-run-all": "^4.1.5"
   }
}
